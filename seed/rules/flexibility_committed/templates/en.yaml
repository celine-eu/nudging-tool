title_jinja: "Load shift committed: {{ window_start }}–{{ window_end }}"
body_jinja: |
  Your commitment to shift loads between {{ window_start }} and {{ window_end }} is registered.
  Estimated reward: {{ reward_points_estimated }} points. We'll confirm once your data is settled.
cta_url: /gamification
